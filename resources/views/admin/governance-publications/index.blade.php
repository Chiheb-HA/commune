@extends('layouts.admin')
@section('page-title', 'Governance Publications')
@section('title', 'Governance Publications')
@section('content')
<div class="page-header"><h1>{{ __('Governance Publications') }}</h1></div><div class="card mb-4"><div class="card-body"><a href="{{ route('admin.governance-publications.create') }}" class="btn btn-primary"><i class="bi bi-plus-circle"></i> {{ __('New Publication') }}</a></div></div><div class="card"><div class="card-body">@if($publications->count())<div class="table-responsive"><table class="table table-hover"><thead><tr><th>{{ __('Title') }}</th><th>{{ __('Type') }}</th><th>{{ __('Publication Date') }}</th><th>{{ __('Active') }}</th><th>{{ __('Actions') }}</th></tr></thead><tbody>@foreach($publications as $publication)<tr><td>{{ $publication->title }}</td><td>{{ $publication->type_label }}</td><td>{{ $publication->publication_date->format('Y-m-d') }}</td><td>{{ $publication->is_active ? __('Active') : __('Inactive') }}</td><td><a href="{{ route('admin.governance-publications.edit', $publication) }}" class="btn btn-sm btn-primary"><i class="bi bi-pencil"></i></a><form action="{{ route('admin.governance-publications.destroy', $publication) }}" method="POST" class="d-inline">@csrf @method('DELETE')<button class="btn btn-sm btn-danger" onclick="return confirm('{{ __('Are you sure?') }}')"><i class="bi bi-trash"></i></button></form></td></tr>@endforeach</tbody></table></div>{{ $publications->links() }}@else<p class="text-muted">{{ __('No governance publications found.') }}</p>@endif</div></div>
@endsection
