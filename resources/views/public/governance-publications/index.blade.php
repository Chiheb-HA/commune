@extends('layouts.app')
@section('title', __('Governance Publications'))
@section('content')
<div class="container-lg py-5"><h1 class="mb-4">{{ __('Governance Publications') }}</h1><form method="GET" class="row g-2 mb-4"><div class="col-md-5"><select name="type" class="form-select"><option value="">{{ __('All types') }}</option>@foreach(['programme_participatif' => 'Programme participatif', 'consultation_publique' => 'Consultation publique', 'pai' => 'PAI', 'pic' => 'PIC', 'pges' => 'PGES'] as $value => $label)<option value="{{ $value }}" @selected(request('type') === $value)>{{ $label }}</option>@endforeach</select></div><div class="col-md-2"><button class="btn btn-primary">{{ __('Filter') }}</button></div></form><div class="row g-4">@forelse($publications as $publication)<div class="col-md-6"><article class="card h-100"><div class="card-body"><span class="badge bg-secondary mb-2">{{ $publication->type_label }}</span><h5>{{ $publication->title }}</h5><p class="text-muted">{{ Str::limit($publication->description, 180) }}</p><small class="text-muted">{{ $publication->publication_date->format('d/m/Y') }}</small>@if($publication->attachment_path)<div class="mt-3"><a href="{{ asset($publication->attachment_path) }}" class="btn btn-sm btn-outline-primary">{{ __('Download') }}</a></div>@endif</div></article></div>@empty<div class="col-12"><div class="alert alert-info">{{ __('No governance publications available.') }}</div></div>@endforelse</div>@if($publications->hasPages())<div class="d-flex justify-content-center mt-5">{{ $publications->links('pagination::bootstrap-5') }}</div>@endif</div>
@endsection
